
const URLs = {
    root: '/',
    auth: {
        login: '/login',
        logout: '/logout',
        profile: '/profile',
        register: '/register'
    },
    api: {
        login: '/login',
        logout: '/logout',
        register: '/register',
        validateToken: '/validate'
    }
}

export default URLs
