
const URLs = {
    root: '/',
    auth: {
        login: '/login',
        logout: '/logout',
        profile: '/profile',
        register: '/register'
    },
    api: {
        login: '/login',
        logout: '/logout',
        register: '/register'
    }
}

export default URLs
